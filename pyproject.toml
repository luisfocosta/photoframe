[project]
name = "photoframe-automation"
version = "0.1.0"
description = "A Python program to automate Edge browser actions using Selenium and handle REST requests with Flask"
authors = ["luisfcosta@live.com"]
dependencies = [
    "dotenv>=0.9.9",
    "flask==3.1.2",
    "selenium==4.2.0",
    "psutil>=5.9.0",
    "requests>=2.31.0",
]
